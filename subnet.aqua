data SubnetWorker:
    pat_id: string
    host_id: string
    worker_id: ?string

data Subnet:
    workers: []SubnetWorker
    error: []string

-- Blockchain query service
service Connector("fluence_aurora_connector"):
    -- Returns a list of workers by deal id
    resolve_subnet(deal_id: string) -> Subnet

